# D1 example (reference only)

このディレクトリは、Drizzle と D1 の最小的な参考コードです。Furima Sandbox の実行時ルート、migration、CI、デプロイには接続していません。

本番・共有環境へコピーする場合は、`db/schema.ts`、`drizzle/`、`app/api/` の現行契約を確認し、独立した migration とテストを用意してください。
